# Edge Function secrets (set via `supabase secrets set`):
# HUNTER_API_KEY=
# OPENAI_API_KEY=
# BING_SEARCH_API_KEY=   # free Azure Bing Search — LinkedIn URL discovery
# SERPER_API_KEY=        # optional paid Google SERP alternative
# PROXYCURL_API_KEY=     # optional paid LinkedIn-shaped people API
# GOOGLE_GMAIL_CLIENT_ID=
# GOOGLE_GMAIL_CLIENT_SECRET=
# APP_ORIGIN=https://YOUR_USER.github.io/FollowUp
# Optional: OSINT_WORKER_URL=   # http://127.0.0.1:8787 — Python worker (theHarvester, etc.)
# Optional: RUN_SEARCH_BUDGET_MS=140000  # internal stop before platform wall clock (max ~150s free tier)
