# Core PoC — install only what you enable via --providers
requests>=2.31.0
beautifulsoup4>=4.12.0
dnspython>=2.6.0
email-validator>=2.1.0

# Optional: pip install theHarvester  (CLI name may vary by distro)
# holehe  # pip install holehe — P2 account check

# Worker (P1)
fastapi>=0.110.0
uvicorn>=0.27.0
